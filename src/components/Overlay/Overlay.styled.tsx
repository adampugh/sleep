import styled from 'styled-components';

export const Overlay = styled.div`
    .overlay {
        position: absolute;
        height: 100vh;
        width: 100vw;
        background: rgba(0, 0, 0, 0.3);
        top: 0;
    }
`;
